{
  "name": "optocloud",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "run-p -l clean:*",
    "clean:svelte": "rimraf .routify dist",
    "build": "run-s -l clean build:svelte",
    "build:svelte": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@roxi/routify": "^3.0.0-next.1",
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.13",
    "@tsconfig/svelte": "^3.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^4.1.2",
    "rollup-plugin-visualizer": "^5.9.0",
    "svelte": "^3.55.1",
    "svelte-check": "^2.10.3",
    "tslib": "^2.5.0",
    "typescript": "^4.9.5",
    "vite": "^3.0.3"
  },
  "browserslist": {
    "production": [
      "> 0.5%",
      "not dead",
      "not op_mini all",
      "supports es6-module"
    ],
    "development": [
      "last 1 version"
    ]
  }
}
